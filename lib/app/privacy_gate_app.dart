import 'package:flutter/material.dart';

class PrivacyGateApp extends StatelessWidget {
  const PrivacyGateApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'PrivacyGate',
      home: const Scaffold(
        body: Center(child: Text('PrivacyGate Mobile')),
      ),
    );
  }
}
